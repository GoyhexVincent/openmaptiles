Run the following command to be able to use your custom style:

docker run -it -v $(pwd):/data -v $(pwd)/styles:/usr/src/app/node_modules/tileserver-gl-styles/styles -p 8080:80 klokantech/tileserver-gl data/tiles.mbtiles --verbose
