Run the following command to be able to use your custom style:

docker run -it -v $(pwd)/data:/data -v $(pwd)/styles/custom:/usr/src/app/node_modules/tileserver-gl-styles/styles/klokantech-basic -p 8080:80 klokantech/tileserver-gl data/tiles.mbtiles --verbose

